% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMultiBinBar.R
\name{plotMultiBinBar}
\alias{plotMultiBinBar}
\title{Graphique en barres horizontales pour variables binaires (choix multiples)}
\usage{
plotMultiBinBar(
  dt,
  cols,
  labels = NULL,
  groupVars = NULL,
  question = NULL,
  outputDir,
  fileFormat = "png",
  barColor = "#662483",
  width = 8,
  height = 5,
  dpi = 300,
  xAxisTitle = NULL,
  graphTitle = NULL,
  subTitle = NULL,
  fontSize = 11,
  labelSize = 3
)
}
\arguments{
\item{dt}{\link[data.table:data.table]{data.table::data.table} Table de données source.}

\item{cols}{\link{character} Noms des colonnes binaires (0/1) à analyser.}

\item{labels}{\link{character} Labels affichés à la place des noms de colonnes. Doit être de même longueur que \code{cols}. Si NULL, les noms de colonnes sont utilisés.}

\item{groupVars}{\link{character} Vecteur de 0, 1 ou 2 noms de variables catégorielles pour le regroupement. Default : NULL.}

\item{question}{Chaîne de caractères. Titre de la question affiché en titre du graphique.}

\item{outputDir}{\link{character} Répertoire de sauvegarde du graphique.}

\item{fileFormat}{\link{character} Format de sortie : "png", "pdf" ou "svg". Default : "png".}

\item{barColor}{\link{character} Couleur des barres. Default : "#662483".}

\item{width}{\link{numeric}  Largeur en pouces. Default : 8.}

\item{height}{\link{numeric}  Hauteur en pouces. Default : 5.}

\item{dpi}{\link{numeric}  Résolution (ignorée pour pdf). Default : 300.}

\item{xAxisTitle}{\link{character} Titre de l'axe X. Default : NULL.}

\item{graphTitle}{\link{character} Titre principal du graphique. Default : NULL.}

\item{subTitle}{\link{character} Sous-titre du graphique. Default : NULL.}

\item{fontSize}{\link{numeric}  Taille de base des textes. Default : 11.}

\item{labelSize}{\link{numeric}  Taille des étiquettes de valeurs sur les barres. Default : 3.}
}
\value{
Invisiblement, le chemin complet du fichier sauvegardé.
}
\description{
Génère un graphique en barres horizontales triées par ordre décroissant de
pourcentage, adapté aux questions à choix multiples (variables 0/1).
Le graphique mentionne explicitement que plusieurs réponses sont possibles
et affiche le N total de répondants.

Sans variable de regroupement, un seul graphique est produit.
Avec une variable de regroupement, un \code{facet_wrap} est utilisé.
Avec deux variables de regroupement, un \code{facet_grid} est utilisé
(première variable en lignes, seconde en colonnes).

Les items sont triés selon leur pourcentage global (sans groupement),
ce qui permet de conserver une référence de lecture cohérente entre
les facettes.
}
\examples{
\dontrun{
dtEx <- data.table::data.table(
  smartphone = sample(0:1, 200, replace = TRUE),
  ordinateur = sample(0:1, 200, replace = TRUE),
  tablette   = sample(0:1, 200, replace = TRUE),
  console    = sample(0:1, 200, replace = TRUE),
  television = sample(0:1, 200, replace = TRUE),
  region     = sample(c("Nord", "Sud"), 200, replace = TRUE),
  genre      = sample(c("Homme", "Femme"), 200, replace = TRUE)
)

# Sans regroupement
plotMultiBinBar(
  dt        = dtEx,
  cols      = c("smartphone", "ordinateur", "tablette"),
  outputDir = "output/graphs"
)

# Avec une variable de regroupement
plotMultiBinBar(
  dt        = dtEx,
  cols      = c("smartphone", "ordinateur", "tablette"),
  groupVars = "genre",
  outputDir = "output/graphs"
)

# Avec deux variables de regroupement
plotMultiBinBar(
  dt        = dtEx,
  cols      = c("smartphone", "ordinateur", "tablette"),
  groupVars = c("genre", "region"),
  outputDir = "output/graphs"
)
}

}
