% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanTextCols.R
\name{cleanTextCols}
\alias{cleanTextCols}
\title{Nettoie des colonnes textuelles dans une data.table avec suivi}
\usage{
cleanTextCols(
  dt,
  vecCols = NULL,
  replaceQuotes = TRUE,
  removeNonAscii = TRUE,
  normalizeSpaces = TRUE,
  showLog = TRUE
)
}
\arguments{
\item{dt}{data.table à modifier par référence.}

\item{vecCols}{vecteur optionnel de colonnes à nettoyer.
Si NULL, toutes les colonnes textuelles sont sélectionnées.}

\item{replaceQuotes}{logique : activer le remplacement des curly quotes.}

\item{removeNonAscii}{logique : activer suppression des caractères non ASCII.}

\item{normalizeSpaces}{logique : activer la normalisation des espaces.}

\item{showLog}{logique : afficher le nombre de valeurs modifiées par colonne.}
}
\value{
invisible(data.table) modifiée par référence. Si showLog=TRUE, affiche un log.
}
\description{
Applique des opérations de nettoyage sur des colonnes :
\itemize{
\item remplacement des guillemets typographiques (curly quotes),
\item suppression des caractères non ASCII,
\item normalisation des espaces,
\item suppression des espaces en début/fin.
}
}
\details{
Optionnellement, retourne un log du nombre de valeurs modifiées par colonne.
Par défaut, toutes les colonnes character/factor sont nettoyées.
}
\examples{
library(data.table)

# --- Données d'exemple avec cas typiques à nettoyer ---
dt <- data.table::data.table(
  nom = c("René\u2019s café", "Alice ", "  Bob"),
  adresse = c("3\u00e8me   rue", "12\u2018avenue\u2019", NA)
)

# --- Nettoyage de toutes les colonnes textuelles (comportement par défaut) ---
funCleanTextCols(dt)

# --- Nettoyage d'une sélection de colonnes uniquement ---
funCleanTextCols(dt, vecCols = "nom")

# --- Sans affichage du log ---
funCleanTextCols(dt, showLog = FALSE)

# --- Nettoyage partiel : uniquement normalisation des espaces et trim ---
funCleanTextCols(
  dt,
  replaceQuotes = FALSE,
  removeNonAscii = FALSE,
  normalizeSpaces = TRUE,
  showLog = TRUE
)

# --- Récupération du résultat (modifié par référence) ---
dtClean <- funCleanTextCols(dt, showLog = FALSE)
}
