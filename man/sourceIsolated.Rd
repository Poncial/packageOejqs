% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sourceIsolated.R
\name{sourceIsolated}
\alias{sourceIsolated}
\title{sourceIsolated: Exécuter un script R dans un environnement temporaire isolé}
\usage{
sourceIsolated(scriptPath, returnObjects = NULL)
}
\arguments{
\item{scriptPath}{Character. Chemin vers le fichier R à exécuter.}

\item{returnObjects}{Character vector. Noms des objets à extraire de l'environnement temporaire.
Si NULL (par défaut), retourne tous les objets créés.}
}
\value{
Une liste contenant les objets spécifiés par \code{returnObjects} (ou tous les objets du script si \code{returnObjects = NULL}).
}
\description{
Cette fonction permet de sourcer un script R dans un environnement séparé,
afin que tous les objets intermédiaires créés par le script ne polluent pas
le workspace global. Seuls les objets explicitement retournés sont extraits.
}
\details{
\itemize{
\item L'environnement temporaire hérite du \code{globalenv()}, ce qui permet d'accéder
aux packages déjà chargés (par ex. via \code{pacman::p_load()}) et aux fonctions globales.
\item Les objets créés dans le script mais non listés dans \code{returnObjects} sont automatiquement jetés.
\item Cette approche évite la pollution du workspace avec des objets intermédiaires.
}
}
\examples{
# Créer un script temporaire pour l'exemple
tmpScript <- tempfile(fileext = ".R")
writeLines(
  c(
    "x_intermediaire <- 42",
    "y_intermediaire <- x_intermediaire * 2",
    "resultatFinal   <- y_intermediaire + 10"
  ),
  con = tmpScript
)

# Cas 1 : extraire un seul objet — les intermédiaires sont ignorés
out <- sourceIsolated(tmpScript, returnObjects = "resultatFinal")
out$resultatFinal # 94

# Cas 2 : extraire tous les objets créés par le script
out <- sourceIsolated(tmpScript)
names(out) # "x_intermediaire" "y_intermediaire" "resultatFinal"

# Vérifier que les intermédiaires ne polluent pas le workspace
exists("x_intermediaire") # FALSE

# Nettoyage
file.remove(tmpScript)

}
