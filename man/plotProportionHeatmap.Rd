% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotProportionHeatmap.R
\name{plotProportionHeatmap}
\alias{plotProportionHeatmap}
\title{Générer une heatmap des proportions d'une variable ordinale selon une variable Likert}
\usage{
plotProportionHeatmap(
  data,
  xVar,
  yVar,
  groupVars = NULL,
  groupLabelForFile = NULL,
  mode = c("proportion", "deviation_uniform"),
  xAxisTitle = NULL,
  yAxisTitle = NULL,
  showTitle = FALSE,
  plotTitle = NULL,
  xValueLabels = NULL,
  yValueLabels = NULL,
  outputDir = "outputs/heatmaps",
  fileFormat = "png",
  width = 10,
  height = 7,
  dpi = 300,
  naRm = TRUE,
  dropUnusedLevels = FALSE,
  fillColors = c("#575783", "#662483", "#951B81", "#E71E73", "#F39200"),
  midpoint = NULL,
  warnUnmapped = TRUE
)
}
\arguments{
\item{data}{\code{data.frame} ou \code{data.table} contenant les variables.}

\item{xVar}{\code{character(1)}. Nom de la variable Likert affichée en abscisse.}

\item{yVar}{\code{character(1)}. Nom de la variable ordinale affichée en ordonnée.}

\item{groupVars}{\code{character}. Vecteur de 0 à 2 variables de regroupement pour les facettes.}

\item{groupLabelForFile}{\code{character(1)} optionnel. Label personnalisé à insérer dans le nom de fichier
(ex. \code{"genre"}). Si \code{NULL}, utilise \code{groupVars} ou \code{"all"}.}

\item{mode}{\code{character(1)}. \code{"proportion"} (défaut) ou \code{"deviation_uniform"}.}

\item{xAxisTitle}{\code{character(1)} optionnel. Titre de l'axe x.}

\item{yAxisTitle}{\code{character(1)} optionnel. Titre de l'axe y.}

\item{showTitle}{\code{logical(1)}. Afficher un titre de graphique (\code{FALSE} par défaut).}

\item{plotTitle}{\code{character(1)} optionnel. Titre personnalisé (utilisé si \code{showTitle = TRUE}).}

\item{xValueLabels}{\code{character} nommé optionnel. Mapping des valeurs de \code{xVar}
(ex. \code{c("1"="Pas du tout d'accord", ...)}).}

\item{yValueLabels}{\code{character} nommé optionnel. Mapping des valeurs de \code{yVar}.}

\item{outputDir}{\code{character(1)}. Dossier de sortie pour l'export.}

\item{fileFormat}{\code{character(1)}. Format de sortie : \code{"png"}, \code{"pdf"}, \code{"jpeg"},
\code{"jpg"}, \code{"tiff"} ou \code{"bmp"}.}

\item{width}{\code{numeric(1)}. Largeur du graphique exporté.}

\item{height}{\code{numeric(1)}. Hauteur du graphique exporté.}

\item{dpi}{\code{numeric(1)}. Résolution pour les formats raster.}

\item{naRm}{\code{logical(1)}. Supprimer les lignes avec NA sur les variables utilisées.}

\item{dropUnusedLevels}{\code{logical(1)}. Argument passé aux facettes ggplot.}

\item{fillColors}{\code{character}. Couleurs de remplissage :
au moins 2 en mode \code{"proportion"}, au moins 3 en mode \code{"deviation_uniform"}.}

\item{midpoint}{\code{numeric(1)} optionnel. Point milieu pour \code{"deviation_uniform"}.
Si \code{NULL}, utilise 0.}

\item{warnUnmapped}{\code{logical(1)}. Afficher un warning si certaines valeurs observées
ne sont pas présentes dans \code{xValueLabels}/\code{yValueLabels}.}
}
\value{
Une liste avec :
\describe{
\item{plot}{Objet \code{ggplot}.}
\item{aggregatedData}{\code{data.table} agrégée (comptages, total, proportion, déviation).}
\item{outputPath}{Chemin complet du fichier exporté.}
\item{fileName}{Nom du fichier exporté.}
\item{mode}{Mode utilisé.}
\item{uniformReference}{Référence uniforme \eqn{1/K} utilisée pour la déviation.}
}
}
\description{
Cette fonction calcule et visualise la distribution conditionnelle de \code{yVar}
au sein de chaque modalité de \code{xVar}, avec possibilité de facettage (0 à 2 variables).
Deux modes sont disponibles :
\itemize{
\item \code{"proportion"} : proportions brutes \eqn{P(y \mid x, groupes)}
\item \code{"deviation_uniform"} : écart à une référence uniforme \eqn{1 / K}
}
}
\details{
Le graphique est exporté automatiquement dans \code{outputDir}.
Le dossier est créé s'il n'existe pas.

Structure du nom de fichier :
\code{valDate_groupLabel_xVar_by_yVar_mode_heatmap.fileFormat}
avec \code{valDate <- format(Sys.Date(), "\%Y\%m\%d")}.
}
\examples{
\dontrun{
labLikert <- c(
  "1" = "Pas du tout d'accord",
  "2" = "Plutôt pas d'accord",
  "3" = "Ni d'accord ni pas d'accord",
  "4" = "Plutôt d'accord",
  "5" = "Tout à fait d'accord"
)

res <- plotProportionHeatmap(
  data = dtEleves,
  xVar = "qid59",
  yVar = "qid67",
  groupVars = "sexe",
  groupLabelForFile = "genre",
  mode = "proportion",
  showTitle = FALSE,
  xAxisTitle = "Niveau d'accord",
  yAxisTitle = "Réponse ordinale",
  xValueLabels = labLikert,
  yValueLabels = labLikert,
  outputDir = "outputs/heatmaps"
)

print(res$plot)
res$outputPath
}

}
