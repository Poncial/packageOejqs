% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPercentBar.R
\name{plotPercentBar}
\alias{plotPercentBar}
\title{Générer des graphiques en barres de pourcentages}
\usage{
plotPercentBar(
  dt,
  cols,
  outputDir,
  groupVars = NULL,
  valueLabels = NULL,
  legendTitle = NULL,
  barColors = c("#F39200", "#E71E73", "#951B81", "#E84E0F", "#C3004D"),
  fileFormat = "png",
  width = 8,
  height = 5,
  dpi = 300,
  xAxisTitle = NULL,
  yAxisTitle = "Pourcentage (\%)",
  wrapWidth = 15,
  angleThreshold = 5,
  stacked = FALSE,
  labelThreshold = 1,
  barWidth = 0.8
)
}
\arguments{
\item{dt}{\code{data.table}. La table de données source. Doit être un objet
\code{data.table}.}

\item{cols}{\code{character}. Vecteur de noms de colonnes pour lesquelles générer
les graphiques. Elles correspondent à une question. Chaque colonne donnera
lieu à un graphique distinct.}

\item{outputDir}{\code{character}. Chemin du répertoire de sortie où les fichiers
seront sauvegardés. Le répertoire est créé automatiquement s'il n'existe
pas.}

\item{groupVars}{\code{character} ou \code{NULL}. Vecteur de regroupement (maximum 2).
Si \code{NULL} (défaut), aucun regroupement n'est
appliqué. Avec une variable, un \code{facet_wrap} est utilisé. Avec deux
variables, un \code{facet_grid} est utilisé (mode non empilé) ou la première
variable est placée en axe x et la seconde en \code{facet_wrap} (mode empilé).}

\item{valueLabels}{\code{character} ou \code{NULL}. Vecteur nommé de labels à
appliquer aux modalités. Les noms doivent correspondre aux valeurs
présentes dans \code{cols}. Si \code{NULL} (défaut), les valeurs brutes sont
utilisées.}

\item{legendTitle}{\code{character} Titre de la légende}

\item{barColors}{\code{character}. Vecteur de couleurs hexadécimales utilisées
pour les barres. Par défaut :
\code{c("#F39200", "#E71E73", "#951B81", "#E84E0F", "#C3004D")}.
Si le nombre de couleurs est inférieur au nombre de modalités, un
avertissement est émis et les couleurs sont recyclées.}

\item{fileFormat}{\code{character}. Format d'export des graphiques. Valeurs
acceptées : \code{"png"} (défaut), \code{"pdf"}, \code{"svg"}, \code{"jpeg"}, \code{"tiff"}.}

\item{width}{\code{numeric}. Largeur du graphique en pouces. Doit être strictement
positif. Par défaut : \code{8}.}

\item{height}{\code{numeric}. Hauteur du graphique en pouces. Doit être
strictement positif. Par défaut : \code{5}.}

\item{dpi}{\code{numeric}. Résolution du graphique en points par pouce. Doit être
strictement positif. Par défaut : \code{300}.}

\item{xAxisTitle}{\code{character} ou \code{NULL}. Titre de l'axe x. Si \code{NULL}
(défaut), le nom de la variable est utilisé.}

\item{yAxisTitle}{\code{character}. Titre de l'axe y. Par défaut :
\code{"Pourcentage (\%)"}.}

\item{stacked}{\code{logical}. Si \code{TRUE}, les barres sont empilées et normalisées
à 100\%. Si \code{FALSE} (défaut), les barres sont groupées côte à côte.}

\item{labelThreshold}{\code{numeric}. Seuil minimal en pourcentage (entre 0 et
100) en dessous duquel les labels ne sont pas affichés sur les segments
en mode empilé (\code{stacked = TRUE}). Par défaut : \code{1}. Sans effet en mode
non empilé.}

\item{barWidth}{largeur des barres. Par défaut = \code{.8}}
}
\value{
Retourne invisiblement une liste de graphiques \code{ggplot2}, un par
élément de \code{cols}. Les graphiques sont également sauvegardés dans
\code{outputDir} avec un nom de fichier structuré comme suit :
\verb{YYYYMMDD_<col>[_by_<groupVars>][_stacked].<fileFormat>}.
}
\description{
Cette fonction génère des graphiques en barres représentant la distribution
en pourcentage des modalités de réponse pour une ou plusieurs variables
d'intérêt. Les graphiques peuvent être groupés par une ou deux variables
de regroupement.
}
\details{
Les valeurs manquantes (\code{NA}) sont exclues du calcul des pourcentages.
Les pourcentages sont calculés au sein de chaque combinaison de
\verb{question x groupVars}.

En mode empilé (\code{stacked = TRUE}), l'axe y est normalisé à 100\% via
\code{position = "fill"}. Les labels des segments inférieurs à \code{labelThreshold}
ne sont pas affichés afin de préserver la lisibilité.
}
\examples{
\dontrun{
library(data.table)
library(ggplot2)

# Création d'un jeu de données exemple
set.seed(42)
dtEx <- data.table(
  q1 = sample(c("Oui", "Non", "NSP"), 200, replace = TRUE),
  q2 = sample(c("Satisfait", "Neutre", "Insatisfait"), 200, replace = TRUE),
  region = sample(c("Nord", "Sud"), 200, replace = TRUE),
  genre = sample(c("Homme", "Femme"), 200, replace = TRUE)
)

# Exemple 1 : graphiques simples sans regroupement
plotPercentBar(
  dt = dtEx,
  cols = c("q1", "q2"),
  outputDir = "output/graphs"
)

# Exemple 2 : graphiques avec une variable de regroupement
plotPercentBar(
  dt = dtEx,
  cols = c("q1", "q2"),
  outputDir = "output/graphs",
  groupVars = "region"
)

# Exemple 3 : graphiques empilés avec deux variables de regroupement
plotPercentBar(
  dt = dtEx,
  cols = c("q1", "q2"),
  outputDir = "output/graphs",
  groupVars = c("region", "genre"),
  stacked = TRUE,
  labelThreshold = 8
)

# Exemple 4 : avec labels personnalisés et couleurs modifiées
plotPercentBar(
  dt = dtEx,
  cols = "q1",
  outputDir = "output/graphs",
  valueLabels = c("Oui" = "Oui", "Non" = "Non", "NSP" = "Ne sait pas"),
  barColors = c("#F39200", "#E71E73", "#951B81"),
  fileFormat = "pdf"
)
}

}
