% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCorrelationHeatmap.r
\name{plotCorrelationHeatmap}
\alias{plotCorrelationHeatmap}
\title{Générer une heatmap de corrélations de Spearman}
\usage{
plotCorrelationHeatmap(
  corDt,
  titre = NULL,
  sousTitre = NULL,
  sigThreshold = 0.05,
  lowColor = "#951B81",
  midColor = "white",
  highColor = "#F39200",
  outputPath,
  width = 9,
  height = 8,
  dpi = 300,
  labelSize = 2.8
)
}
\arguments{
\item{corDt}{Un \code{data.table} au format long, tel que retourné par
\code{\link[=computeSpearmanMatrix]{computeSpearmanMatrix()}}. Doit contenir au minimum les colonnes
\code{labelVar1}, \code{labelVar2}, \code{rho} et \code{pValue}.}

\item{titre}{Chaîne de caractères. Titre principal du graphique. Défaut
\code{NULL} (aucun titre).}

\item{sousTitre}{Chaîne de caractères. Sous-titre du graphique. Défaut
\code{NULL}.}

\item{sigThreshold}{Seuil de significativité utilisé pour marquer les
coefficients d'un astérisque. Défaut \code{0.05}.}

\item{lowColor}{Couleur hexadécimale utilisée pour les corrélations
négatives extrêmes (\code{rho = -1}). Défaut \code{"#951B81"}.}

\item{midColor}{Couleur hexadécimale utilisée pour une corrélation nulle
(\code{rho = 0}). Défaut \code{"white"}.}

\item{highColor}{Couleur hexadécimale utilisée pour les corrélations
positives extrêmes (\code{rho = 1}). Défaut \code{"#F39200"}.}

\item{outputPath}{Chemin complet du fichier de sortie (incluant le nom de
fichier et l'extension, p.ex. \code{".png"}). Le répertoire est créé
automatiquement s'il n'existe pas.}

\item{width}{Largeur du fichier exporté en pouces. Défaut \code{9}.}

\item{height}{Hauteur du fichier exporté en pouces. Défaut \code{8}.}

\item{dpi}{Résolution du fichier exporté en points par pouce. Défaut
\code{300}.}

\item{labelSize}{Taille du texte affichant les coefficients sur les
tuiles. Défaut \code{2.8}.}
}
\value{
Retourne invisiblement l'objet \code{ggplot} généré. Le graphique est
également sauvegardé sur disque à l'emplacement \code{outputPath}.
}
\description{
Génère et sauvegarde une heatmap représentant une matrice de corrélations,
typiquement produite par \code{\link[=computeSpearmanMatrix]{computeSpearmanMatrix()}}. Les coefficients
significatifs (\code{p < .05}) sont marqués d'un astérisque. Le dégradé de
couleurs utilise par défaut la charte graphique OEJQS.
}
\details{
L'ordre d'affichage des variables sur les axes suit l'ordre d'apparition
dans \code{corDt} (déterminé par l'ordre de \code{cols} lors de l'appel à
\code{\link[=computeSpearmanMatrix]{computeSpearmanMatrix()}}), et non un tri automatique — ceci permet de
contrôler explicitement le regroupement thématique des variables à
l'affichage.
}
\examples{
\dontrun{
library(data.table)

set.seed(123)
dtEx <- data.table(
  reussite   = sample(1:5, 200, replace = TRUE),
  stress     = sample(1:5, 200, replace = TRUE),
  dependance = sample(1:5, 200, replace = TRUE)
)

corResult <- computeSpearmanMatrix(
  dt     = dtEx,
  cols   = c("reussite", "stress", "dependance"),
  labels = c("Réussite perçue", "Stress scolaire", "Dépendance smartphone")
)

plotCorrelationHeatmap(
  corDt      = corResult,
  titre      = "Corrélations entre réussite, stress et dépendance",
  outputPath = tempfile(fileext = ".png")
)
}

}
\seealso{
\code{\link[=computeSpearmanMatrix]{computeSpearmanMatrix()}} pour générer \code{corDt}.
}
